import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import AllotSeatsPage from '@/component/member/AllotSeat';

const Index = () => {
    return (
        <AllotSeatsPage />
    );
}

const styles = StyleSheet.create({})

export default Index;
